/**
 * push method is used for intesrt the element at the end of the array.
 * this methos we should give argument in the method
 * this method gives the length of array
 */

const arr = [1, 2, 3, 'and']

arr.push(4)

console.log(arr)

