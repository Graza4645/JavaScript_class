/**
 * this method converts array into a String.
 * array is object datatype
 */

let arr = [8,9,10,11];
console.log(typeof(arr));
let newArr = arr.join()
console.log(arr);
console.log(typeof(newArr));
