/**
 * this method converts array into a String.
 * array is object datatype
 */

let arr = [8,9,10,11];
console.log(typeof(arr));
let newArr = arr.join()
console.log(arr);
console.log(typeof(newArr));

// We can also use toString for the convert from array to String

const arr1 = [5, 7, 3, 5, 6, 9]

console.log(arr1);
console.log(arr1.toString());