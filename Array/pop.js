/**
 * this method is used for remove the last element of an array
 * this method rgives the element which is removed
 * there is no need to provide argument
 */

const arr = ['vikas', 'pal'];

let newArr = arr.pop();
console.log(newArr);
console.log(arr);

