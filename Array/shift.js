/**
 * this method is used to remove the Element from the begining of the array
 * it gives the output of the element which is removed
 * it ignore the arguments
 */

const arr = [4, 5, 6, 7, ];

arr.shift();

console.log(arr);