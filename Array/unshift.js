/**
 * this method is used for insert new element  at the begining of an array
 * this returns the length the of array
 * it accept the argument
 */

const arr = [2, 3, 4];

let newArr= arr.unshift(1)
console.log(newArr)
console.log(arr);